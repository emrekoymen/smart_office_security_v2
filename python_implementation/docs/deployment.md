# Deployment to Raspberry Pi

## 1. Pi OS Preparation
- OS image  
- SSH & display setup

## 2. Install Dependencies
- Python & pip  
- Coral runtime for ARM  
- OpenCV build or apt

## 3. Project Transfer
- `git clone` / rsync  
- Activate venv & install

## 4. Run Modes
- Display vs headless  
- Systemd service example

## Python Prototype

- Tested on Ubuntu with Conda
- For Raspberry Pi: install ARM pycoral, OpenCV, and dependencies as in `environment.yml`
- Use `--headless` for headless operation